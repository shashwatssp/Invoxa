# Backend env example. Values intentionally blank; copy to `.env` at the
# repo root or supply via your hosting platform's secret manager.
#
# Required:
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_KEY=

# Optional:
SUPABASE_JWKS_URL=
GEMINI_API_KEY=
DATABASE_URL=
DEBUG=false
