name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install -r backend/requirements-dev.txt

      - name: Syntax check
        run: |
          python -m py_compile backend/app/config.py
          python -m py_compile backend/app/supabase.py
          python -m py_compile backend/app/main.py
          python -m py_compile backend/app/models/invoice.py

      - name: Lint with ruff
        run: ruff check backend/app/

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Type check
        working-directory: ./frontend
        run: npx tsc --noEmit

      - name: Build
        working-directory: ./frontend
        shell: bash
        run: |
          set -o pipefail
          npm run build 2>&1 | tee /tmp/build.log || {
            tail -10 /tmp/build.log | while IFS= read -r line; do
              echo "::error::$line"
            done
            exit 1
          }

  secret-scan:
    runs-on: ubuntu-latest
    steps:
      # Full history is required: gitleaks-action scans the pushed commit range
      # and fails (exit 1) when the range's first commit is missing from a
      # shallow clone.
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install gitleaks
        uses: gitleaks/gitleaks-action@v2
