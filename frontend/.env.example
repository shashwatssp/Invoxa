# Frontend environment for Invoxa.
#
# Copy this file to `.env.local` for local development, or set the value in
# your hosting provider's dashboard. Never commit real secrets.
#
# Public URL of the deployed backend. Leave blank during local development
# to use relative URLs (the Vite dev server proxies /api to localhost:8000).
VITE_API_BASE_URL=
